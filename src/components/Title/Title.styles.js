import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 22,
        fontWeight: '900'
    },
    icon: {
        fontSize: 20,
        fontWeight: 'bold',
        marginRight: 15,
    }
})

export default styles