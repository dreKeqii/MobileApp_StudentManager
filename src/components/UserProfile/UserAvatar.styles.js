import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    avatar: {
        width: 45,
        height: 45,
        borderRadius: 100,
        borderWidth: 1,
        borderColor: 'purple',
    },
    fallbackIcon: {
        fontSize: 38,
    }
})

export default styles